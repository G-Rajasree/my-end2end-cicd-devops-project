apiVersion: v1
kind: Service
metadata:
  name: github-trending-service
  namespace: galaxy
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: LoadBalancer
  selector:
    app: github-trending
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
